# sum_digit
This C# console app uses a recursive method to calculate the sum of digits of a user-entered number.   👉 The program asks for input, processes each digit through recursion, and prints the total result.✨
